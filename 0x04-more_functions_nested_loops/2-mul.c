#include <stdio.h>

/**
 * mul - Multiplies two integers
 * @x: the firsr integer
 * @y: the second integer
 * Return: the result of multiplication betwwen x and y
 */

int mul(int x, int y)

{
	return (x * y);
}
